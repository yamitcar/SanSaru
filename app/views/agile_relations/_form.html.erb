<%= form_with(model: agile_relation, local: true) do |form| %>
  <% if agile_relation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agile_relation.errors.count, "error") %> prohibited this agile_relation from being saved:</h2>

      <ul>
      <% agile_relation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :agile_relation_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<div id="reset-password" class="access-section">
  <div class="title-element">
    <p>¿No puedes acceder? quizás</p> 
    <h1>Olvidaste tu contraseña</h1>
  </div>
  <div class="inner-title-element">
    <p>No te preocupes</p> 
    <h1>Restauremos tu acceso</h1>
  </div>

  <div class="align-items-center">
    <div class="col-md-8 offset-md-4">
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= devise_error_messages! %>
        <fieldset>
          <div class="form-group">
            <label class="col-md-6 control-label" for="textinput">
              <%= f.label :email, "Ingresa tu E-mail" %>
            </label>
            <div class="col-md-6">
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control input-md' %>
            </div>
          </div>

          <!-- Button -->
          <div class="form-group text-center">
            <div class="col-md-6">
              <%= f.submit "Restaurar contraseña", class: "btn btn-primary" %>
            </div>
          </div>
        </fieldset>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>